# gif_search_app

A gif search application using the Giphy service.

## Flutter Version

Flutter 3.10.0-110.0.dev

## Environment Setup

1. Create a `.env` file in the project root by copying from `.env.example`.
2. Replace the placeholder values in `.env` with your actual API keys and other necessary configurations.
